/**
 * Enumeration dos tipos de cursor
 * @export
 * @enum {string}
 */
export enum Cursor {
    POINTER = "pointer",
    HELP = "help",
    WAIT = "wait",
    CROSSHAIR = "crosshair",
    NOT_ALLOWED = "not-allowed",
    ZOOM_IN = "zoom-in",
    GRAB = "grab",
    AUTO = "auto",
}