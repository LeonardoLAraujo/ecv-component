/**
 * A paleta de cor extra do Material Design que contem as cores branco e preto.
 * @export
 * @type {PalleteColorShade}
 */
export type PalleteColorShade = {
    /**
     * Representa a palheta de cor white
     * @type {string} 
     */
    white: string,
    /**
     * Representa a palheta de cor black
     * @type {string} 
     */
    black: string
}