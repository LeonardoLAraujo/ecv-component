import { PalleteColor } from "./pallete-color";
import { PalleteColorShade } from "./pallete-color-shade";

/**
 * A palheta de cor do Material Design.
 * @export
 * @type {MaterialColorPallete}
 */
export type MaterialColorPallete = {
    /**
     * Representa a palheta de cor red
     * @type {PaletteColor} 
     */
    red: PalleteColor,
    /**
     * Representa a palheta de cor pink
     * @type {PaletteColor} 
     */
    pink: PalleteColor,
    /**
     * Representa a palheta de cor purple
     * @type {PaletteColor} 
     */
    purple: PalleteColor,
    /**
     * Representa a palheta de cor deepPurple
     * @type {PaletteColor} 
     */
    deepPurple: PalleteColor,
    /**
     * Representa a palheta de cor indigo
     * @type {PaletteColor} 
     */
    indigo: PalleteColor,
    /**
     * Representa a palheta de cor blue
     * @type {PaletteColor} 
     */
    blue: PalleteColor,
    /**
     * Representa a palheta de cor lightBlue
     * @type {PaletteColor} 
     */
    lightBlue: PalleteColor,
    /**
     * Representa a palheta de cor cyan
     * @type {PaletteColor} 
     */
    cyan: PalleteColor,
    /**
     * Representa a palheta de cor teal
     * @type {PaletteColor} 
     */
    teal: PalleteColor,
    /**
     * Representa a palheta de cor green
     * @type {PaletteColor} 
     */
    green: PalleteColor,
    /**
     * Representa a palheta de cor lightGreen
     * @type {PaletteColor} 
     */
    lightGreen: PalleteColor,
    /**
     * Representa a palheta de cor lime
     * @type {PaletteColor} 
     */
    lime: PalleteColor,
    /**
     * Representa a palheta de cor yellow
     * @type {PaletteColor} 
     */
    yellow: PalleteColor,
    /**
     * Representa a palheta de cor amber
     * @type {PaletteColor} 
     */
    amber: PalleteColor,
    /**
     * Representa a palheta de cor orange
     * @type {PaletteColor} 
     */
    orange: PalleteColor,
    /**
     * Representa a palheta de cor deepOrange
     * @type {PaletteColor} 
     */
    deepOrange: PalleteColor,
    /**
     * Representa a palheta de cor brow
     * @type {PaletteColor} 
     */
    brow: PalleteColor,
    /**
     * Representa a palheta de cor grey
     * @type {PaletteColor} 
     */
    grey: PalleteColor,
    /**
     * Representa a palheta de cor blueGrey
     * @type {PaletteColor} 
     */
    blueGrey: PalleteColor,
    /**
     * Representa a palheta de cor shade
     * @type {PaletteColor} 
     */
    shade: PalleteColorShade
}